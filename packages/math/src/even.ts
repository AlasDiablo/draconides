/**
 * Check if the number is even
 * @param value - Number to check
 * @returns - Returns true if the value is dividable by 2
 * @version 0.0.2
 * @since 0.0.2
 */
const even = (value: number): boolean => {
    return value % 2 === 0;
};

export default even;
