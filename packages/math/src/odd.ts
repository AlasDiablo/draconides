/**
 * Check if the number is odd
 * @param value - Number to check
 * @returns - Returns true if the value is not dividable by 2
 * @version 0.0.2
 * @since 0.0.2
 */
const odd = (value: number): boolean => {
    return value % 2 !== 0;
};

export default odd;
